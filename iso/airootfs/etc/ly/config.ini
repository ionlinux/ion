# Ion Linux — ly display manager config (live ISO)
allow_empty_password = true
animation = none

# Autologin for live session
auto_login_user = liveuser
auto_login_session = Hyprland (uwsm-managed)
auto_login_service = ly-autologin
