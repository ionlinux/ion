# Ion Linux — Live ISO Hyprland config
# Sources the base config and adds Calamares installer

source = /etc/calamares/hyprland.conf

exec-once = sudo -E calamares

windowrule {
    name = float-calamares
    match:class = calamares
    float = true
    center = true
    size = 1024 600
}
